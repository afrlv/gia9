services:
  gia9-monitor:
    build: .
    container_name: gia9-monitor
    restart: unless-stopped
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - gia9-data:/data

volumes:
  gia9-data:
